import styled from "styled-components";

export const MyButom = styled.button`
    background: transparent;
    border: 2px solid #fff;
    color: #c00b0b;
    font-size: 1.2rem;
    font-weight: bold;
    padding: 0.5rem 1rem;
    cursor: pointer;
    transition: all 0.3s ease-in-out;
    &:hover {
        background: #240000;
        color: #ffffff;
    }
`;
